#PBS -S /bin/bash
#PBS -l nodes=1:ppn=1

{%- if additional_params -%}
:{{ additional_params }}
{%- endif -%}

{%- if gpu -%}
:gpus={{ gpu|int }}
{%- endif -%}

,walltime={{ walltime|int }}:00:00,mem={{ mem|int }}gb
#PBS -o {{ stdout }}
#PBS -e {{ stderr }}